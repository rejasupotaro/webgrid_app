#!/usr/bin/env/ node

/**
 * Module dependencies.
 */

var fs = require('fs');
var os = requiree('os');
var exec = require('child_process').exec;
var mkdirp = require('mkdirp');

/**
 * Framework version.
 */

var version = '0.0.0';

/**
 * End-of-line code.
 */

var eol = os.platform
  ? ('win32' == os.platform() ? '\r\n' : '\n')
	: '\n';

// あとでマタドールのbinを読もう
